// ─────────────────────────────────────────────
// app/engine/constants.ts
// ─────────────────────────────────────────────

/** Grid columns */
export const COLS = 8

/** Grid rows */
export const ROWS = 6

/** Height of bottom dock in px (fixed) */
export const DOCK_HEIGHT = 76

/** Height of top status bar in px (fixed) */
export const STATUS_BAR_HEIGHT = 36

/** Pixels from left/right edge that triggers page-swipe while dragging */
export const PAGE_SWIPE_ZONE = 80

/** Milliseconds before long-press enters edit mode */
export const LONG_PRESS_MS = 600

/** How close (fraction of cell) two apps must be to show folder hint */
export const FOLDER_OVERLAP_FRACTION = 0.6

/** Resize handle size in px */
export const RESIZE_HANDLE_PX = 28

/** App icon padding inside one cell (px each side) */
export const APP_ICON_PAD = 6

/** Corner radius for glass cards */
export const CARD_RADIUS = 20

/** App icon border radius as fraction of cell */
export const APP_RADIUS_FRACTION = 0.22